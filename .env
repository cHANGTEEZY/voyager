# Better Auth server root (no trailing slash).
# Expo reads this file from the project root — not src/.env
#
# Android emulator: localhost is auto-rewritten to 10.0.2.2
# Physical device: set your machine's LAN IP, e.g. http://192.168.1.42:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
